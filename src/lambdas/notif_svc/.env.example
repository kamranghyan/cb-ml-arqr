# .env.example — copy to .env, never commit .env

# ── Required ──────────────────────────────────────────────────────────────────
SNS_TOPIC=arn:aws:sns:ap-south-1:833090513377:orders-notifications-dev
WS_ENDPOINT=https://6zvolh5t5b.execute-api.ap-south-1.amazonaws.com/prod
TABLE_ORDER=OrderTable

# ── Optional (features disabled if blank) ────────────────────────────────────
WS_CONNECTIONS_TABLE=ConnectionTable-dev
PINPOINT_APP_ID=
PINPOINT_FROM_NUMBER=

# ── AWS ───────────────────────────────────────────────────────────────────────
AWS_REGION=ap-south-1
AWS_PROFILE=default

# ── Deploy script ─────────────────────────────────────────────────────────────
DEPLOY_BUCKET=arqr-dev-sam-artifacts
SNS_TOPIC_ARN=arn:aws:sns:ap-south-1:833090513377:orders-notifications-dev
ORDER_TABLE_NAME=OrderTable
WS_CONNECTIONS_TABLE_NAME=ConnectionTable-dev
